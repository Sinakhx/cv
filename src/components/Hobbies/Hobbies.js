import React from "react";
import PropTypes from "prop-types";

import "./Hobbies.scss";

export const Hobbies = (props) => {
  return <section className="hobbies">hobbies</section>;
};

Hobbies.propTypes = {
  props: PropTypes.object,
};
